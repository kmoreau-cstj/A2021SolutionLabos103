double calculerCarre(double valeur)
{
   // le paramètre valeur est une variable de type double qui contient déjà un nombre, elle est initialisée
   // Déclarer des variables
   double resultat;
   
   resultat = valeur * valeur;

   // On rend public le résultat, on retourne le résultat
   return resultat;
}
